# Contributing to CPOT

This project is under early development.

## Guidelines
- Follow existing naming conventions
- Public functions go in `src/CPOT/Public`
- Private helpers go in `src/CPOT/Private`
- Tests must be added under `tests/unit`
- Functions should return objects, not formatted output
``